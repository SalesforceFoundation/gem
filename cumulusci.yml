project:
    dependencies:
        - github: https://github.com/SalesforceFoundation/Cumulus
        - github: https://github.com/SalesforceFoundation/HEDAP
    name: Advancement
    package:
        name: Gift Entry Manager
        namespace: gem
        api_version: 42.0
    git:
        prefix_release: rel/gem/
        prefix_beta: beta/gem/
        repo_url: https://github.com/SalesforceFoundation/gem

tasks:
    deploy_abacus_settings:
        description: Configure the default Abacus Settings for Gift Processing
        class_path: tasks.anon_apex.AnonymousApexFromFileTask
        options:
            path: unpackaged/config/scripts/configure_abacus_settings.cls
            method: initializeAbacusSettings();

    deploy_unpackaged/config:
        description: Deploys configuration for Development. Assigns page layouts, compact layouts, and sets tab visibilities. Record type visibilities are set the update_admin_profile task
        class_path: cumulusci.tasks.salesforce.DeployBundles
        options:
            namespace_inject: $project_config.project__package__namespace
            path: unpackaged/config/src
            unmanaged: True

    deploy_installer_config:
        description: Deploys configuration for managed install. Assigns page layouts, compact layouts, and sets tab visibilities.
        class_path: cumulusci.tasks.salesforce.Deploy
        options:
            namespace_inject: $project_config.project__package__namespace
            path: unpackaged⁩/config/installer
            unmanaged: True

    deploy_unpackaged/config_delete:
        description: Removes Development configuration. Sets page layouts, compact layouts to system defaults. Removes record type visibilites.
        class_path: cumulusci.tasks.salesforce.Deploy
        options:
            path: unpackaged/config/delete

    deploy_pilot_config:
        description: Deploys configuration for Development. Assigns page layouts, compact layouts, and sets tab visibilities. Record type visibilities are set the update_admin_profile task
        class_path: cumulusci.tasks.salesforce.DeployBundles
        options:
            namespace_inject: $project_config.project__package__namespace
            path: pilot_config
            unmanaged: False

    execute_install_apex:
        description: Runs most of the install script methods from Post_Install_Script class
        class_path: cumulusci.tasks.apex.anon.AnonymousApexTask
        options:
            apex: >
                Post_Install_Script.giftSetup();
                Post_Install_Script.populateNPSPAccountModel();

    execute_installer_settings:
        description: Configure the default Settings for Gift Processing
        class_path: tasks.anon_apex.AnonymousApexFromFileTask
        options:
            path: unpackaged/config/scripts/configure_default_settings.cls
            method: setupGEMDefaults();

    execute_installer_settings_npsp_defaults:
        description: Configure the default Settings for Gift Processing
        class_path: tasks.anon_apex.AnonymousApexFromFileTask
        options:
            path: unpackaged/config/scripts/configure_default_settings.cls
            method: setupGEMAndNPSPDefaults();

    test_data_dev_org:
        description: 'Loads a test data set for most NPSP objects based on 100 Contacts that should fit into a scratch org or DE org'
        class_path: cumulusci.tasks.bulkdata.LoadData
        options:
            database_url: 'sqlite:///testdata/dev_org/test_data.db'
            mapping: 'testdata/mapping.yml'

    test_data_delete:
        description: 'WARNING: Deletes all data in the objects specified in the objects option.'
        class_path: cumulusci.tasks.bulkdata.DeleteData
        options:
            objects:
                - Opportunity
                - npe03__Recurring_Donation__c
                - Case
                - Contact
                - Account
                - npsp__Allocation__c
                - npsp__General_Accounting_Unit__c
                - Campaign

    test_data_default_settings:
        description: Configure the default Settings for Gift Processing, including NPSP and EDA changes
        class_path: tasks.anon_apex.AnonymousApexFromFileTask
        options:
            path: unpackaged/config/scripts/configure_default_settings.cls
            method: setupGEMDefaultsForDev();

    test_data_user_setup:
        description: Sets up Gift Processing Users with correct Profiles
        class_path: tasks.anon_apex.AnonymousApexFromFileTask
        options:
            path: unpackaged/config/scripts/configure_default_settings.cls
            method: AddGiftProcessor();

flows:
    config_apextest:
        steps:
            3:
                task: deploy_dev_config

    config_dev:
        steps:
            3:
                task: deploy_dev_config_delete
            4:
                task: deploy_dev_config
            5:
                task: deploy_installer_config
            6:
                flow: test_data_setup

    config_qa:
        steps:
            2:
                task: None
            3:
                task: deploy_dev_config_delete
            4:
                task: deploy_dev_config
            5:
                task: deploy_installer_config
            6:
                task: update_admin_profile
            7:
                flow: test_data_dev_org
            8:
                flow: test_data_setup

    config_managed:
        steps:
            3:
                task: deploy_dev_config
                options:
                    unmanaged: False
            4:
                task: deploy_installer_config
                options:
                    unmanaged: False

    config_installer:
        steps:
            1:
                task: deploy_post
                options:
                    unmanaged: False
            2:
                task: update_admin_profile
                options:
                    managed: True
            3:
                task: deploy_installer_config
                options:
                    unmanaged: False

    ci_feature:
        steps:
            7.1:
                task: run_tests
                options:
                   managed: True
                   namespace: hed
            7.2:
                task: run_tests
                options:
                    managed: True
                    namespace: npsp

    ci_feature_beta_deps:
        description: When run, the latest version (including betas) of Cumulus and HEDA are installed, and all tests are run.
        steps:
            5:
                task: run_tests
                options:
                   managed: True
                   namespace: hed
            6:
                task: run_tests
                options:
                    managed: True
                    namespace: npsp

    dev_org_beta_deps:
        description: Set up an org as a development environment for unmanaged metadata based on the latest version (including betas).
        steps:
            1:
                flow: beta_dependencies
            2:
                flow: deploy_unmanaged
            3:
                flow: config_dev

    test_data_setup:
        description: 'WARNING: This flow deletes all data first, then loads the complete test data set based on 100 Contacts into the target org.'
        steps:
            1:
                task: test_data_default_settings
            2:
                task: test_data_user_setup
            3:
                task: execute_install_apex

    test_data_dev_org:
        description: 'WARNING: This flow deletes all data first, then loads the complete test data set based on 100 Contacts into the target org.'
        steps:
            1:
                task: test_data_delete
            2:
                task: test_data_dev_org

    install_pilot:
      steps:
          1:
              task: update_dependencies
              options:
                  dependencies:
                      - github: https://github.com/SalesforceFoundation/Cumulus
                      - github: https://github.com/SalesforceFoundation/HEDAP
                      - github: https://github.com/SalesforceFoundation/Abacus
          2:
              task: deploy_pre
          3:
              task: install_managed
          4:
              flow: config_managed
          5:
              flow: install_pilot_settings
          6:
              task: deploy_pilot_config

    install_gem:
      steps:
          1:
              task: update_dependencies
              options:
                  dependencies:
                      - github: https://github.com/SalesforceFoundation/Cumulus
                      - github: https://github.com/SalesforceFoundation/HEDAP
                      - github: https://github.com/SalesforceFoundation/Abacus
          2:
              task: deploy_pre
          3:
              task: install_managed
          4:
              flow: config_installer
          5:
              flow: install_gem_settings
          6:
              task: deploy_pilot_config

    install_pilot_settings:
        steps:
            1:
                task: test_data_default_settings
                options:
                    managed: True
            2:
                task: deploy_abacus_settings
                options:
                    managed: True

    install_gem_settings:
        steps:
            1:
                task: execute_installer_settings_npsp_defaults
                options:
                    managed: True
            2:
                task: deploy_abacus_settings
                options:
                    managed: True


    # Temporary workaround for deployment issue introduced in 1c00b3c
    deploy_packaging:
        steps:
            6:
                options:
                    ignore:
                        FlexiPage:
                            - Gift_Entry
                        CustomTab:
                            - Gift_Entry
                        CustomField:
                            - npsp__DataImport__c.Donation_Primary_Campaign__c

plans:
    limited_release:
        slug: limited-release
        title: Install Limited Release
        tier: primary
        description: Installs the Gift Entry Manager Limited Release
        steps:
            1:
                task: update_dependencies
                options:
                    dependencies:
                        - github: https://github.com/SalesforceFoundation/Cumulus
                        - github: https://github.com/SalesforceFoundation/HEDAP
                        - github: https://github.com/SalesforceFoundation/Abacus
                ui_options:
                    1:
                        name: NPSP - Account Record Types
                    2:
                        name: NPSP - Opportunity Record Types
                    9:
                        name: NPSP Config for Salesforce Mobile App
                    10:
                        name: HEDA - Account Record Types
                    11:
                        name: HEDA - Contact Key Affiliation Fields
                    13:
                        name: HEDA - Deploy Course Connection Record Types
            2:
                task: deploy_pre
            3:
                task: install_managed
            4:
                flow: config_managed
            5:
                flow: install_pilot_settings
                ui_options:
                    test_data_default_settings:
                        name: Configure the default NPSP Settings for Gift Processing
                    deploy_abacus_settings:
                        name: Deploy Abacus Settings

orgs:
    scratch:
        dev_namespaced:
            config_file: orgs/dev.json
            namespaced: True

        dev_prerelease:
            config_file: orgs/dev_prerelease.json
