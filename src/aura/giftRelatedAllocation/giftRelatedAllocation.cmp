<aura:component extends="c:giftFormRelatedRow">
    <aura:attribute name="item" 
        type="npsp__Allocation__c" 
        default="{sobjectType:'npsp__Allocation__c'}"/>

    <div class="slds-grid slds-wrap">
        <div class="slds-col slds-size_2-of-4 slds-p-around_small">
            <aura:if isTrue="{!v.showLabels}">
                <label class="show-required slds-form-element__label">General Accounting Unit</label>
            </aura:if>
            <force:inputField aura:id="noDuplicates"
                value="{!v.item.npsp__General_Accounting_Unit__c}" />
        </div>
        <div class="slds-col slds-size_1-of-4 slds-p-around_small">
            <aura:if isTrue="{!v.showLabels}">
                <label class="show-required slds-form-element__label">Amount</label>
            </aura:if>
            <lightning:input aura:id="requiredField"
                onblur="{!c.handleAmountCheck}"
                name="allocationAmt" 
                type="number" 
                label="Amount" variant="label-hidden"
                formatter="currency"
                disabled="{!v.item.npsp__Percent__c ? true : false}"
                value="{!v.item.npsp__Amount__c}" />
        </div>
        <div class="slds-col slds-size_1-of-4 slds-p-around_small">
            <aura:if isTrue="{!v.showLabels}">
                <label class="slds-form-element__label">Percent</label>
            </aura:if>
            <lightning:input onblur="{!c.handlePercentChange}"
                name="allocationPerc" 
                type="number" 
                label="Percent" variant="label-hidden"
                max="100" min="0" 
                value="{!v.item.npsp__Percent__c}" />
        </div>

        <div class="slds-hidden">
            <lightning:input name="oppId" type="text" label="opp" value="{!v.item.npsp__Opportunity__c}" />
        </div>
    </div>

</aura:component>